# GPU Phase 1 measurements — 2026-07-17

## Phase 1c parity: PASS (measured)

Same-seed evaluate-snr at -31 dB shelf, seed 20260715, 200 trials,
--noise-source python, backend cuda (A100 MIG 2g.20gb slice, kernel
v1.0.0, CuPy 14.1.1, driver 570.172.08 / CUDA 12.8). Independently
verified in-session from the uploaded run (parity_gpu_20260717.zip):

- max|F_gpu - F_cpu_packed| = 0.0 over all 200 trials
- max|F_gpu - F_cpu_reference| = 0.0 over all 200 trials
- detection decisions identical trial-by-trial: 126/200 backup-mode
  (fixed-tau) on both; 197/200 positive-excess
- per-trial noise seeds recorded in dtv_snr_eval.json

Manuscript: Phase 1c red box converted to measured text in §5.
Consequence: every CPU-reference result in the paper (45k-trial sweep,
float control, upcoming GPU sweeps) is the deployed kernel's behaviour
by measurement, not by CI assertion alone.

Incidental single-point note: gpu_snr_error_db = -0.509 at -31 dB on the
UNTRIMMED capture - the detector-side shelf estimate reads low by about
the amount the 0.72 dB pilot deficit predicts through the composite
chain (sign and rough magnitude consistent with §5.1's calibration
envelope; not a precision measurement).

## Still pending in Phase 1

- 1a control certification (chime-controls scans; the pilot-free floor)
- GPU sweep suite G3 (trimmed pilot) and injection ladder G4 (Phase 2)

## R8 refinements (same day)

Paired-bootstrap CIs on the int4-float control (500 reps): thrP50
-0.047 [-0.121,+0.022], thrP90 -0.022 [-0.100,+0.051], pexP90 -0.063
[-0.238,+0.121] dB (95%). Parity artifact gaps noted: schema records
gpu-side integers + rational_overflow_count (=0 across 200 trials) but
not CPU-side integers nor producing commit / weight-bank hash; Dylan to
commit the artifact to the public repo and record commit+hash.

## Weight-bank identity (2026-07-17)

GPU session hash: b0dce17a8ef5b24da1fa9d7e41bab019531a06549bd8eb20ce54e9a9c77e173b
weights/chime_dtv_weights_k128.bin — EXACT MATCH to the notebook1
fig3-era provenance/hashes.sha256 entry. The CPU 45k-trial sweep and the
GPU parity run used byte-identical weight banks (cross-machine
identity). Producing commit for the parity/robustness era: 8f840b6.
Remaining from R8's provisional rule: commit results/parity_gpu/ to the
public repository.

## Phase 1c: CLOSED (R8 criterion met)

Parity artifact committed and pushed to the public repository at
data/provenance/parity_gpu_20260715seed (gitignore carve-out added,
mirroring data/census). With the paired records public, the producing
commit chain recorded, and the weight bank hash-matched across machines,
Phase 1c meets R8's stated closure criterion in full.
